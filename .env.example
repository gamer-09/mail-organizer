# Optional. You can also paste these in Settings inside the app.
# Never commit real secrets. These are public OAuth client IDs (not client secrets).

GOOGLE_CLIENT_ID=
MICROSOFT_CLIENT_ID=
PORT=4173
